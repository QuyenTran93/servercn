schema: spec-driven

context: |
  Language: Vietnamese
  Write in Vietnamese, but:
  - Keep technical terms like "API", "REST", "GraphQL" in English
  - Code examples and file paths remain in English
  We use conventional commits

rules:
  tasks:
    - Break tasks into chunks of max 2 hours
